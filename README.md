# 🌊 Ocean Aquarium Equipment Control System

Система автоматического управления оборудованием океанариума.

## 📋 Требования

| Требование | Версия |
|------------|--------|
| Windows | 8 или выше |
| Python | 3.10+ |
| Сеть | Доступ к сети оборудования |

## 🚀 Быстрый старт

### Вариант 1: Установка с интернетом

```batch
# 1. Запустите установку зависимостей
scripts\install.bat

# 2. Отредактируйте конфигурацию
notepad config.json

# 3. Запустите систему
scripts\start.bat
```

### Вариант 2: Установка без интернета

**На компьютере с интернетом:**
```batch
scripts\download_packages.bat
```

**На целевом компьютере (без интернета):**
```batch
scripts\install_offline.bat
scripts\start.bat
```

### 4. Откройте веб-интерфейс

```
http://localhost:8000
```

## 📁 Структура проекта

```
Ocean/
├── main.py                 # Точка входа
├── config.json             # Конфигурация
├── requirements.txt        # Зависимости Python
├── README.md               # Этот файл
│
├── scripts/                # Batch-файлы
│   ├── start.bat           # Запуск системы
│   ├── install.bat         # Онлайн-установка
│   ├── install_offline.bat # Оффлайн-установка
│   ├── download_packages.bat # Скачивание пакетов
│   └── test.bat            # Запуск тестов
│
├── docs/                   # Документация
│   ├── ARCHITECTURE.md     # Архитектура системы
│   └── DEPLOYMENT.md       # Инструкция по развёртыванию
│
├── app/
│   ├── core/               # Ядро системы
│   ├── protocols/          # Протоколы устройств
│   ├── services/           # Сервисы
│   └── static/             # Веб-интерфейс
│
├── tests/                  # Тесты
├── packages/               # Оффлайн-пакеты
├── logs/                   # Логи (создаётся автоматически)
└── data/                   # Данные (создаётся автоматически)
```

## ⚙️ Конфигурация

Отредактируйте `config.json`:

```json
{
  "schedule": {
    "on_time": "09:00",
    "off_time": "20:00",
    "timezone": "Asia/Vladivostok"
  },
  "devices": [
    {
      "id": "optoma_2.64",
      "name": "Optoma 2.64",
      "ip": "192.168.2.64",
      "port": 23,
      "device_type": "optoma_telnet",
      "group_id": "projectors_optoma",
      "enabled": true
    }
  ]
}
```

## 🧪 Тестирование

```batch
scripts\test.bat
```

## 🔧 Troubleshooting

### Устройство не включается

1. Проверьте IP в `config.json`
2. Проверьте сетевое подключение: `ping 192.168.2.64`
3. Посмотрите логи в веб-интерфейсе (вкладка Logs)

### Веб-интерфейс не открывается

1. Убедитесь, что `scripts\start.bat` запущен
2. Проверьте порт: `netstat -ano | findstr :8000`
3. Попробуйте http://127.0.0.1:8000

## 📊 API Endpoints

| Метод | URL | Описание |
|-------|-----|----------|
| GET | /api/health | Статус системы |
| GET | /api/devices | Список устройств |
| POST | /api/devices/{id}/on | Включить устройство |
| POST | /api/devices/{id}/off | Выключить устройство |
| POST | /api/devices/all/on | Включить все |
| POST | /api/devices/all/off | Выключить все |
| GET | /api/schedule | Расписание |
| GET | /api/alerts | Алерты |
| GET | /api/logs | Логи |

## 📝 Документация

- [Архитектура системы](docs/ARCHITECTURE.md)
- [Инструкция по развёртыванию](docs/DEPLOYMENT.md)

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в веб-интерфейсе
2. Перезапустите систему (`Ctrl+C`, затем `scripts\start.bat`)
3. Проверьте `config.json` на синтаксические ошибки
