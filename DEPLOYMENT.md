# 🚀 Руководство по развёртыванию

## Требования

| Компонент | Требование |
|-----------|------------|
| ОС | Windows 8/10/11 (64-bit) |
| Python | 3.10 или выше |
| RAM | Минимум 512 MB |
| Сеть | Доступ к сети оборудования |
| Порт | 8000 (или другой свободный) |

## Схема развёртывания

```
┌─────────────────────────────────────────────────────────────┐
│                    КОМПЬЮТЕР С ИНТЕРНЕТОМ                   │
│                                                             │
│  1. Скачать проект Ocean                                    │
│  2. Запустить download_packages.bat                        │
│  3. Скопировать папку Ocean на флешку                      │
│                                                             │
└───────────────────────────┬─────────────────────────────────┘
                            │ USB / Network
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                ЦЕЛЕВОЙ КОМПЬЮТЕР (БЕЗ ИНТЕРНЕТА)            │
│                                                             │
│  1. Скопировать папку Ocean                                 │
│  2. Установить Python 3.10+ (если не установлен)           │
│  3. Запустить install_offline.bat                          │
│  4. Настроить config.json                                   │
│  5. Запустить start.bat                                    │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## Пошаговая инструкция

### Шаг 1: Подготовка (на компьютере с интернетом)

```batch
# 1. Клонируйте или скачайте проект
# 2. Откройте папку Ocean
# 3. Запустите скачивание пакетов
download_packages.bat
```

После выполнения появится папка `packages/` с .whl файлами.

### Шаг 2: Перенос на целевой компьютер

1. Скопируйте **всю папку Ocean** на флешку
2. Перенесите на целевой компьютер
3. Рекомендуемое расположение: `C:\Ocean\`

### Шаг 3: Установка Python (если не установлен)

> **Важно**: Python должен быть установлен с галочкой "Add Python to PATH"

Для установки Python без интернета:
1. Скачайте установщик с python.org на компьютере с интернетом
2. Перенесите на целевой компьютер
3. Запустите установку с параметрами:
   - ✅ Add Python to PATH
   - ✅ Install for all users (рекомендуется)

Проверка:
```batch
python --version
# Должно вывести: Python 3.10.x или выше
```

### Шаг 4: Оффлайн-установка зависимостей

```batch
install_offline.bat
```

Скрипт автоматически:
1. Создаст виртуальное окружение (venv)
2. Установит все пакеты из папки `packages/`

### Шаг 5: Настройка конфигурации

Откройте `config.json` и настройте:

```json
{
  "app": {
    "port": 8000,              // Порт веб-интерфейса
    "debug": false
  },
  "schedule": {
    "on_time": "09:00",        // Время включения
    "off_time": "20:00",       // Время выключения
    "timezone": "Asia/Vladivostok"
  },
  "devices": [
    {
      "id": "optoma_2.64",
      "ip": "192.168.2.64",    // ← Укажите реальные IP
      ...
    }
  ]
}
```

### Шаг 6: Запуск

```batch
start.bat
```

Откройте браузер: **http://localhost:8000**

## Автозапуск при старте Windows

### Вариант 1: Планировщик задач

1. Откройте **Task Scheduler** (taskschd.msc)
2. Create Task:
   - **General**: Name = "Ocean Aquarium Control"
   - **Trigger**: At startup
   - **Action**: Start program = `C:\Ocean\start.bat`
   - **Conditions**: ❌ Start only if on AC power

### Вариант 2: Папка автозагрузки

1. Win+R → `shell:startup`
2. Создайте ярлык на `start.bat`

## Проверка работы

### 1. Проверка веб-интерфейса
```
http://localhost:8000
```

### 2. Проверка API
```batch
curl http://localhost:8000/api/health
```

### 3. Проверка подключения к устройствам
```batch
ping 192.168.2.64
```

## Troubleshooting

### "Python не найден"
```batch
# Проверьте PATH
where python

# Если не найден, добавьте вручную:
setx PATH "%PATH%;C:\Python310"
```

### "Ошибка импорта модуля"
```batch
# Переустановите зависимости
install_offline.bat
```

### "Порт занят"
```batch
# Найдите процесс
netstat -ano | findstr :8000

# Убейте по PID
taskkill /PID <PID> /F
```

### "Устройство не отвечает"
1. Проверьте IP в config.json
2. Проверьте сетевое подключение: `ping <IP>`
3. Проверьте порт устройства (23 для Telnet, 9090 для Barco)

## Обновление системы

1. Остановите текущий сервер (Ctrl+C)
2. Скопируйте новые файлы поверх старых
3. При изменении зависимостей:
   - На компьютере с интернетом: `download_packages.bat`
   - На целевом: `install_offline.bat`
4. Запустите: `start.bat`

## Резервное копирование

Рекомендуется регулярно копировать:
- `config.json` — конфигурация
- `data/` — база планировщика и отчёты
- `logs/` — логи действий
